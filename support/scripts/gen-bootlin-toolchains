#!/usr/bin/env python3

import os.path
import re
import requests
import textwrap
import sys

BASE_URL = "https://toolchains.bootlin.com/downloads/releases/toolchains"

AUTOGENERATED_COMMENT = """# This file was auto-generated by support/scripts/gen-bootlin-toolchains
# Do not edit
"""

# In the below dict:

# - 'conditions' indicate the cumulative conditions under which the
#   toolchain will be made available. In several situations, a given
#   toolchain is usable on several architectures variants (for
#   example, an ARMv6 toolchain can be used on ARMv7)
# - 'test_options' indicate one specific configuration where the
#   toolchain can be used. It is used to create the runtime test
#   cases. If 'test_options' does not exist, the code assumes it can
#   be made equal to 'conditions'
# - 'prefix' is the prefix of the cross-compilation toolchain tools

arches = {
    'aarch64': {
        'conditions': ['LINGMO_aarch64'],
        'prefix': 'aarch64',
    },
    'aarch64be': {
        'conditions': ['LINGMO_aarch64_be'],
        'prefix': 'aarch64_be',
    },
    'arcle-750d': {
        'conditions': ['LINGMO_arcle', 'LINGMO_arc750d'],
        'prefix': 'arc',
    },
    'arcle-hs38': {
        'conditions': ['LINGMO_arcle', 'LINGMO_archs38'],
        'prefix': 'arc',
    },
    'armv5-eabi': {
        'conditions': ['LINGMO_arm', 'LINGMO_ARM_CPU_ARMV5', 'LINGMO_ARM_EABI'],
        'test_options': ['LINGMO_arm', 'LINGMO_arm926t', 'LINGMO_ARM_EABI'],
        'prefix': 'arm',
    },
    'armv6-eabihf': {
        'conditions': ['LINGMO_arm', 'LINGMO_ARM_CPU_ARMV6', 'LINGMO_ARM_EABIHF'],
        'test_options': ['LINGMO_arm', 'LINGMO_arm1176jzf_s', 'LINGMO_ARM_EABIHF'],
        'prefix': 'arm',
    },
    'armv7-eabihf': {
        'conditions': ['LINGMO_arm', 'LINGMO_ARM_CPU_ARMV7A', 'LINGMO_ARM_EABIHF'],
        'test_options': ['LINGMO_arm', 'LINGMO_cortex_a8', 'LINGMO_ARM_EABIHF'],
        'prefix': 'arm',
    },
    'armebv7-eabihf': {
        'conditions': ['LINGMO_armeb', 'LINGMO_ARM_CPU_ARMV7A', 'LINGMO_ARM_EABIHF'],
        'test_options': ['LINGMO_armeb', 'LINGMO_cortex_a8', 'LINGMO_ARM_EABIHF'],
        'prefix': 'armeb',
    },
    'armv7m': {
        'conditions': ['LINGMO_arm', 'LINGMO_ARM_CPU_ARMV7M'],
        'test_options': ['LINGMO_arm', 'LINGMO_cortex_m4'],
        'prefix': 'arm',
    },
    'm68k-68xxx': {
        'conditions': ['LINGMO_m68k_m68k'],
        'test_options': ['LINGMO_m68k', 'LINGMO_m68k_68040'],
        'prefix': 'm68k',
    },
    'm68k-coldfire': {
        'conditions': ['LINGMO_m68k_cf'],
        'test_options': ['LINGMO_m68k', 'LINGMO_m68k_cf5208'],
        'prefix': 'm68k',
    },
    'microblazebe': {
        'conditions': ['LINGMO_microblazebe'],
        'prefix': 'microblaze',
        'gdbserver': False
    },
    'microblazeel': {
        'conditions': ['LINGMO_microblazeel'],
        'prefix': 'microblazeel',
        'gdbserver': False
    },
    'mips32': {
        # Not sure it could be used by other mips32 variants?
        'conditions': ['LINGMO_mips', 'LINGMO_mips_32', '!LINGMO_MIPS_SOFT_FLOAT'],
        'prefix': 'mips',
    },
    'mips32el': {
        # Not sure it could be used by other mips32el variants?
        'conditions': ['LINGMO_mipsel', 'LINGMO_mips_32', '!LINGMO_MIPS_SOFT_FLOAT'],
        'prefix': 'mipsel',
    },
    'mips32r5el': {
        'conditions': ['LINGMO_mipsel', 'LINGMO_mips_32r5', '!LINGMO_MIPS_SOFT_FLOAT'],
        'prefix': 'mipsel',
    },
    'mips32r6el': {
        'conditions': ['LINGMO_mipsel', 'LINGMO_mips_32r6', '!LINGMO_MIPS_SOFT_FLOAT'],
        'prefix': 'mipsel',
    },
    'mips64-n32': {
        # Not sure it could be used by other mips64 variants?
        'conditions': ['LINGMO_mips64', 'LINGMO_mips_64', 'LINGMO_MIPS_NABI32', '!LINGMO_MIPS_SOFT_FLOAT'],
        'prefix': 'mips64',
    },
    'mips64el-n32': {
        # Not sure it could be used by other mips64el variants?
        'conditions': ['LINGMO_mips64el', 'LINGMO_mips_64', 'LINGMO_MIPS_NABI32', '!LINGMO_MIPS_SOFT_FLOAT'],
        'prefix': 'mips64el',
    },
    'mips64r6el-n32': {
        'conditions': ['LINGMO_mips64el', 'LINGMO_mips_64r6', 'LINGMO_MIPS_NABI32', '!LINGMO_MIPS_SOFT_FLOAT'],
        'prefix': 'mips64el',
    },
    'nios2': {
        'conditions': ['LINGMO_nios2'],
        'prefix': 'nios2',
    },
    'openrisc': {
        'conditions': ['LINGMO_or1k'],
        'prefix': 'or1k',
        'gdbserver': False,
    },
    'powerpc-440fp': {
        # Not sure it could be used by other powerpc variants?
        'conditions': ['LINGMO_powerpc', 'LINGMO_powerpc_440fp'],
        'prefix': 'powerpc',
    },
    'powerpc-e300c3': {
        # Not sure it could be used by other powerpc variants?
        'conditions': ['LINGMO_powerpc', 'LINGMO_powerpc_e300c3'],
        'prefix': 'powerpc',
    },
    'powerpc-e500mc': {
        # Not sure it could be used by other powerpc variants?
        'conditions': ['LINGMO_powerpc', 'LINGMO_powerpc_e500mc'],
        'prefix': 'powerpc',
    },
    'powerpc64-e5500': {
        'conditions': ['LINGMO_powerpc64', 'LINGMO_powerpc_e5500'],
        'prefix': 'powerpc64',
    },
    'powerpc64-e6500': {
        'conditions': ['LINGMO_powerpc64', 'LINGMO_powerpc_e6500'],
        'prefix': 'powerpc64',
    },
    'powerpc64-power8': {
        'conditions': ['LINGMO_powerpc64', 'LINGMO_powerpc_power8'],
        'prefix': 'powerpc64',
    },
    'powerpc64le-power8': {
        'conditions': ['LINGMO_powerpc64le', 'LINGMO_powerpc_power8'],
        'prefix': 'powerpc64le',
    },
    'riscv32-ilp32d': {
        'conditions': ['LINGMO_riscv', 'LINGMO_riscv_g', 'LINGMO_RISCV_32', 'LINGMO_RISCV_ABI_ILP32D'],
        'prefix': 'riscv32',
    },
    'riscv64-lp64d': {
        'conditions': ['LINGMO_riscv', 'LINGMO_riscv_g', 'LINGMO_RISCV_64', 'LINGMO_RISCV_ABI_LP64D', 'LINGMO_USE_MMU'],
        'prefix': 'riscv64',
    },
    's390x-z13': {
        'conditions': ['LINGMO_s390x', 'LINGMO_s390x_z13'],
        'prefix': 's390x',
    },
    'sh-sh4': {
        'conditions': ['LINGMO_sh', 'LINGMO_sh4'],
        'prefix': 'sh4',
    },
    'sh-sh4aeb': {
        'conditions': ['LINGMO_sh', 'LINGMO_sh4aeb'],
        'prefix': 'sh4aeb',
    },
    'sparc64': {
        'conditions': ['LINGMO_sparc64', 'LINGMO_sparc_v9'],
        'prefix': 'sparc64',
    },
    'sparcv8': {
        'conditions': ['LINGMO_sparc', 'LINGMO_sparc_v8'],
        'prefix': 'sparc',
    },
    'x86-64': {
        'conditions': ['LINGMO_x86_64',
                       'LINGMO_X86_CPU_HAS_MMX',
                       'LINGMO_X86_CPU_HAS_SSE',
                       'LINGMO_X86_CPU_HAS_SSE2'],
        'test_options': ['LINGMO_x86_64', 'LINGMO_x86_x86_64'],
        'prefix': 'x86_64',
    },
    'x86-64-v2': {
        'conditions': ['LINGMO_x86_64',
                       'LINGMO_X86_CPU_HAS_MMX',
                       'LINGMO_X86_CPU_HAS_SSE',
                       'LINGMO_X86_CPU_HAS_SSE2',
                       'LINGMO_X86_CPU_HAS_SSE3',
                       'LINGMO_X86_CPU_HAS_SSSE3',
                       'LINGMO_X86_CPU_HAS_SSE4',
                       'LINGMO_X86_CPU_HAS_SSE42'],
        'test_options': ['LINGMO_x86_64', 'LINGMO_x86_x86_64_v2'],
        'prefix': 'x86_64',
    },
    'x86-64-v3': {
        'conditions': ['LINGMO_x86_64',
                       'LINGMO_X86_CPU_HAS_MMX',
                       'LINGMO_X86_CPU_HAS_SSE',
                       'LINGMO_X86_CPU_HAS_SSE2',
                       'LINGMO_X86_CPU_HAS_SSE3',
                       'LINGMO_X86_CPU_HAS_SSSE3',
                       'LINGMO_X86_CPU_HAS_SSE4',
                       'LINGMO_X86_CPU_HAS_SSE42',
                       'LINGMO_X86_CPU_HAS_AVX',
                       'LINGMO_X86_CPU_HAS_AVX2'],
        'test_options': ['LINGMO_x86_64', 'LINGMO_x86_x86_64_v3'],
        'prefix': 'x86_64',
    },
    'x86-64-v4': {
        'conditions': ['LINGMO_x86_64',
                       'LINGMO_X86_CPU_HAS_MMX',
                       'LINGMO_X86_CPU_HAS_SSE',
                       'LINGMO_X86_CPU_HAS_SSE2',
                       'LINGMO_X86_CPU_HAS_SSE3',
                       'LINGMO_X86_CPU_HAS_SSSE3',
                       'LINGMO_X86_CPU_HAS_SSE4',
                       'LINGMO_X86_CPU_HAS_SSE42',
                       'LINGMO_X86_CPU_HAS_AVX',
                       'LINGMO_X86_CPU_HAS_AVX2',
                       'LINGMO_X86_CPU_HAS_AVX512'],
        'test_options': ['LINGMO_x86_64', 'LINGMO_x86_x86_64_v4'],
        'prefix': 'x86_64',
    },
    'x86-64-core-i7': {
        'conditions': ['LINGMO_x86_64',
                       'LINGMO_X86_CPU_HAS_MMX',
                       'LINGMO_X86_CPU_HAS_SSE',
                       'LINGMO_X86_CPU_HAS_SSE2',
                       'LINGMO_X86_CPU_HAS_SSE3',
                       'LINGMO_X86_CPU_HAS_SSSE3',
                       'LINGMO_X86_CPU_HAS_SSE4',
                       'LINGMO_X86_CPU_HAS_SSE42'],
        'test_options': ['LINGMO_x86_64', 'LINGMO_x86_corei7'],
        'prefix': 'x86_64',
    },
    'x86-core2': {
        'conditions': ['LINGMO_i386',
                       'LINGMO_X86_CPU_HAS_MMX',
                       'LINGMO_X86_CPU_HAS_SSE',
                       'LINGMO_X86_CPU_HAS_SSE2',
                       'LINGMO_X86_CPU_HAS_SSE3',
                       'LINGMO_X86_CPU_HAS_SSSE3'],
        'test_options': ['LINGMO_i386', 'LINGMO_x86_core2'],
        'prefix': 'i686',
    },
    'x86-i686': {
        'conditions': ['LINGMO_i386',
                       '!LINGMO_x86_i486',
                       '!LINGMO_x86_i586',
                       '!LINGMO_x86_x1000',
                       '!LINGMO_x86_pentium_mmx',
                       '!LINGMO_x86_geode',
                       '!LINGMO_x86_c3',
                       '!LINGMO_x86_winchip_c6',
                       '!LINGMO_x86_winchip2'],
        'test_options': ['LINGMO_i386',
                         'LINGMO_x86_i686'],
        'prefix': 'i686',
    },
    'xtensa-lx60': {
        'conditions': ['LINGMO_xtensa', 'LINGMO_XTENSA_CUSTOM', 'LINGMO_XTENSA_LITTLE_ENDIAN'],
        'prefix': 'xtensa',
    },
}


class Toolchain:
    def __init__(self, arch, libc, variant, version):
        self.arch = arch
        self.libc = libc
        self.variant = variant
        self.version = version
        self.fname_prefix = "%s--%s--%s-%s" % (self.arch, self.libc, self.variant, self.version)
        self.option_name = "LINGMO_TOOLCHAIN_EXTERNAL_BOOTLIN_%s_%s_%s" % \
            (self.arch.replace("-", "_").upper(), self.libc.upper(), self.variant.replace("-", "_").upper())
        self.fragment = requests.get(self.fragment_url).text.split("\n")
        self.sha256 = requests.get(self.hash_url).text.split(" ")[0]

    @property
    def tarball_url(self):
        return os.path.join(BASE_URL, self.arch, "tarballs",
                            self.fname_prefix + ".tar.bz2")

    @property
    def hash_url(self):
        return os.path.join(BASE_URL, self.arch, "tarballs",
                            self.fname_prefix + ".sha256")

    @property
    def fragment_url(self):
        return os.path.join(BASE_URL, self.arch, "fragments",
                            self.fname_prefix + ".frag")

    def gen_config_in_options(self, f):
        f.write("config %s\n" % self.option_name)
        f.write("\tbool \"%s %s %s %s\"\n" %
                (self.arch, self.libc, self.variant, self.version))
        depends = []
        selects = []

        for c in arches[self.arch]['conditions']:
            depends.append(c)

        if not arches[self.arch].get('gdbserver', True):
            selects.append("LINGMO_TOOLCHAIN_EXTERNAL_HAS_NO_GDBSERVER")

        for frag in self.fragment:
            # libc type
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_CUSTOM_UCLIBC"):
                selects.append("LINGMO_TOOLCHAIN_EXTERNAL_UCLIBC")
            elif frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_CUSTOM_GLIBC"):
                # glibc needs mmu support
                if "LINGMO_USE_MMU" not in depends:
                    depends.append("LINGMO_USE_MMU")
                selects.append("LINGMO_TOOLCHAIN_EXTERNAL_GLIBC")
            elif frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_CUSTOM_MUSL"):
                # musl needs mmu support
                if "LINGMO_USE_MMU" not in depends:
                    depends.append("LINGMO_USE_MMU")
                selects.append("LINGMO_TOOLCHAIN_EXTERNAL_MUSL")

            # gcc version
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_GCC_"):
                m = re.match("^LINGMO_TOOLCHAIN_EXTERNAL_GCC_([0-9_]*)=y$", frag)
                assert m, "Cannot get gcc version for toolchain %s" % self.fname_prefix
                selects.append("LINGMO_TOOLCHAIN_GCC_AT_LEAST_%s" % m[1])
                # respect the GCC requirement for the selected CPU/arch tuning
                depends.append("!LINGMO_ARCH_NEEDS_GCC_AT_LEAST_%s" % str(int(m[1]) + 1))

            # kernel headers version
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_HEADERS_"):
                m = re.match("^LINGMO_TOOLCHAIN_EXTERNAL_HEADERS_([0-9_]*)=y$", frag)
                assert m, "Cannot get kernel headers version for toolchain %s" % self.fname_prefix
                selects.append("LINGMO_TOOLCHAIN_HEADERS_AT_LEAST_%s" % m[1])

            # C++
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_CXX"):
                selects.append("LINGMO_INSTALL_LIBSTDCPP")

            # SSP
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_HAS_SSP"):
                selects.append("LINGMO_TOOLCHAIN_HAS_SSP")

            # wchar
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_WCHAR"):
                selects.append("LINGMO_USE_WCHAR")

            # locale
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_LOCALE"):
                # locale implies the availability of wchar
                selects.append("LINGMO_USE_WCHAR")
                selects.append("LINGMO_ENABLE_LOCALE")

            # thread support
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_HAS_THREADS"):
                selects.append("LINGMO_TOOLCHAIN_HAS_THREADS")

            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_HAS_THREADS_DEBUG"):
                selects.append("LINGMO_TOOLCHAIN_HAS_THREADS_DEBUG")

            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_HAS_THREADS_NPTL"):
                selects.append("LINGMO_TOOLCHAIN_HAS_THREADS_NPTL")

            # RPC
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_INET_RPC"):
                selects.append("LINGMO_TOOLCHAIN_HAS_NATIVE_RPC")

            # D language
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_DLANG"):
                selects.append("LINGMO_TOOLCHAIN_HAS_DLANG")

            # fortran
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_FORTRAN"):
                selects.append("LINGMO_TOOLCHAIN_HAS_FORTRAN")

            # OpenMP
            if frag.startswith("LINGMO_TOOLCHAIN_EXTERNAL_OPENMP"):
                selects.append("LINGMO_TOOLCHAIN_HAS_OPENMP")

        for depend in depends:
            f.write("\tdepends on %s\n" % depend)

        for select in selects:
            f.write("\tselect %s\n" % select)

        f.write("\thelp\n")

        desc = "Bootlin toolchain for the %s architecture, using the %s C library. " % \
            (self.arch, self.libc)

        if self.variant == "stable":
            desc += "This is a stable version, which means it is using stable and proven versions of gcc, gdb and binutils."
        else:
            desc += "This is a bleeding-edge version, which means it is using the latest versions of gcc, gdb and binutils."

        f.write(textwrap.fill(desc, width=62, initial_indent="\t  ", subsequent_indent="\t  ") + "\n")
        f.write("\n")
        f.write("\t  https://toolchains.bootlin.com/\n")

        f.write("\n")

    def gen_mk(self, f):
        f.write("ifeq ($(%s),y)\n" % self.option_name)
        f.write("TOOLCHAIN_EXTERNAL_BOOTLIN_VERSION = %s\n" % self.version)
        f.write("TOOLCHAIN_EXTERNAL_BOOTLIN_SOURCE = %s--%s--%s-$(TOOLCHAIN_EXTERNAL_BOOTLIN_VERSION).tar.bz2\n" %
                (self.arch, self.libc, self.variant))
        f.write("TOOLCHAIN_EXTERNAL_BOOTLIN_SITE = %s\n" %
                os.path.join(BASE_URL, self.arch, "tarballs"))
        f.write("endif\n\n")
        pass

    def gen_hash(self, f):
        f.write("# From %s\n" % self.hash_url)
        f.write("sha256  %s  %s\n" % (self.sha256, os.path.basename(self.tarball_url)))

    def gen_test(self, f):
        if self.variant == "stable":
            variant = "Stable"
        else:
            variant = "BleedingEdge"
        testname = "TestExternalToolchainBootlin" + \
            self.arch.replace("-", "").capitalize() + \
            self.libc.capitalize() + variant
        f.write("\n\n")
        f.write("class %s(TestExternalToolchain):\n" % testname)
        f.write("    config = \"\"\"\n")
        if 'test_options' in arches[self.arch]:
            test_options = arches[self.arch]['test_options']
        else:
            test_options = arches[self.arch]['conditions']
        for opt in test_options:
            if opt.startswith("!"):
                f.write("        # %s is not set\n" % opt[1:])
            else:
                f.write("        %s=y\n" % opt)
        f.write("        LINGMO_TOOLCHAIN_EXTERNAL=y\n")
        f.write("        LINGMO_TOOLCHAIN_EXTERNAL_BOOTLIN=y\n")
        f.write("        %s=y\n" % self.option_name)
        f.write("        # LINGMO_TARGET_ROOTFS_TAR is not set\n")
        f.write("        \"\"\"\n")
        f.write("    toolchain_prefix = \"%s-linux\"\n" % arches[self.arch]['prefix'])
        f.write("\n")
        f.write("    def test_run(self):\n")
        f.write("        TestExternalToolchain.common_check(self)\n")

    def __repr__(self):
        return "Toolchain(arch=%s libc=%s variant=%s version=%s, option=%s)" % \
            (self.arch, self.libc, self.variant, self.version, self.option_name)


def get_toolchains():
    toolchains = list()
    for arch, details in arches.items():
        print(arch)
        url = os.path.join(BASE_URL, arch, "available_toolchains")
        page = requests.get(url).text
        fnames = sorted(re.findall(r'<td><a href="(\w[^"]+)"', page))
        # This dict will allow us to keep only the latest version for
        # each toolchain.
        tmp = dict()
        for fname in fnames:
            parts = fname.split('--')
            assert parts[0] == arch, "Arch does not match: %s vs. %s" % (parts[0], arch)
            libc = parts[1]
            if parts[2].startswith("stable-"):
                variant = "stable"
                version = parts[2][len("stable-"):]
            elif parts[2].startswith("bleeding-edge-"):
                variant = "bleeding-edge"
                version = parts[2][len("bleeding-edge-"):]
            tmp[(arch, libc, variant)] = version

        if len(tmp) == 0:
            print("ERROR: no toolchains found for architecture %s" % arch)
            sys.exit(1)

        toolchains += [Toolchain(k[0], k[1], k[2], v) for k, v in tmp.items()]

    return toolchains


def gen_config_in_options(toolchains, fpath):
    with open(fpath, "w") as f:
        f.write(AUTOGENERATED_COMMENT)

        f.write("config LINGMO_TOOLCHAIN_EXTERNAL_BOOTLIN_ARCH_SUPPORTS\n")
        f.write("\tbool\n")
        for arch, details in arches.items():
            conditions = details['conditions'].copy()
            f.write("\tdefault y if %s\n" % " && ".join(conditions))
        f.write("\n")

        f.write("if LINGMO_TOOLCHAIN_EXTERNAL_BOOTLIN\n\n")

        f.write("config LINGMO_TOOLCHAIN_EXTERNAL_PREFIX\n")
        f.write("\tdefault \"$(ARCH)-linux\"\n")

        f.write("\n")

        f.write("config LINGMO_PACKAGE_PROVIDES_TOOLCHAIN_EXTERNAL\n")
        f.write("\tdefault \"toolchain-external-bootlin\"\n")

        f.write("\n")

        f.write("choice\n")
        f.write("\tprompt \"Bootlin toolchain variant\"\n")

        for toolchain in toolchains:
            toolchain.gen_config_in_options(f)

        f.write("endchoice\n")
        f.write("endif\n")


def gen_mk(toolchains, fpath):
    with open(fpath, "w") as f:
        f.write("#" * 80 + "\n")
        f.write("#\n")
        f.write("# toolchain-external-bootlin\n")
        f.write("#\n")
        f.write("#" * 80 + "\n")
        f.write("\n")
        f.write(AUTOGENERATED_COMMENT)
        for toolchain in toolchains:
            toolchain.gen_mk(f)
        f.write("$(eval $(toolchain-external-package))\n")


def gen_hash(toolchains, fpath):
    with open(fpath, "w") as f:
        f.write(AUTOGENERATED_COMMENT)
        for toolchain in toolchains:
            toolchain.gen_hash(f)


def gen_runtime_test(toolchains, fpath):
    with open(fpath, "w") as f:
        f.write(AUTOGENERATED_COMMENT)
        f.write("from tests.toolchain.test_external import TestExternalToolchain\n")
        for toolchain in toolchains:
            toolchain.gen_test(f)


def gen_toolchains(toolchains):
    maindir = "toolchain/toolchain-external/toolchain-external-bootlin"
    gen_config_in_options(toolchains, os.path.join(maindir, "Config.in.options"))
    gen_mk(toolchains, os.path.join(maindir, "toolchain-external-bootlin.mk"))
    gen_hash(toolchains, os.path.join(maindir, "toolchain-external-bootlin.hash"))
    gen_runtime_test(toolchains,
                     os.path.join("support", "testing", "tests", "toolchain", "test_external_bootlin.py"))


toolchains = get_toolchains()
gen_toolchains(toolchains)
