# Builds the lingmo-kwin-plugins-roundedwindow source package from this LingmoOS aggregate repository.
# The independent component repository has its own copy under .github/workflows/.
name: Build and upload lingmo-kwin-plugins-roundedwindow

permissions:
  contents: read

on:
  push:
    branches: [main]
    paths:
      - 'windows/global/**'
      - '.github/workflows/lingmo-kwin-plugins-roundedwindow.yml'
    tags:
      - 'lingmo-kwin-plugins-roundedwindow-v*'
  pull_request:
    branches: [main]
    paths:
      - 'windows/global/**'
      - '.github/workflows/lingmo-kwin-plugins-roundedwindow.yml'
  workflow_dispatch:

jobs:
  source-package:
    name: Debian Testing source package
    runs-on: ubuntu-latest
    container: docker.io/library/debian:testing
    steps:
      - name: Install packaging tools
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            ca-certificates \
            curl \
            debhelper \
            devscripts \
            dpkg-dev \
            git

      - name: Checkout source
        uses: actions/checkout@v4

      - name: Build Debian source package
        working-directory: 'windows/global'
        run: dpkg-buildpackage -S -us -uc -d

      - name: Collect source package files
        working-directory: 'windows/global'
        run: |
          mkdir -p source-package
          find .. -maxdepth 1 -type f \
            \( -name '*.dsc' -o -name '*.tar.*' \) \
            -exec mv -t source-package {} +
          test -n "$(find source-package -maxdepth 1 -type f -name '*.dsc' -print -quit)"

      - name: Upload source package to OBS
        if: github.event_name != 'pull_request'
        uses: LingmoOS/action-upload-to-obs@0.0.4
        with:
          remove-old-sources: true
          obs-package-name: 'lingmo-kwin-plugins-roundedwindow'
          obs-project-name: 'home:LingmoOS:Lithium'
          obs-user-name: ${{ secrets.OBS_USERNAME }}
          obs-password: ${{ secrets.OBS_TOKEN }}
          obs-instance-url: 'https://api.opensuse.org'
          local-package-dir: 'windows/global/source-package'
