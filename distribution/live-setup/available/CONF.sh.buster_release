# Config for live and openstack builds

CODENAME="buster"

# Normal release build
#VERSION="10.13.0"

# Dated openstack update - this MUST NOT be used for live builds as
# the version is too long and will cause xorriso to fail!
VERSION="10.13.16-20230701"
