#d-i
# The checkout directory l10n/material/packages.vcs1 *must* exist before,
# containing a checkout of the base d-i git repository
# (created with "git clone git@salsa.debian.org:installer-team/d-i.git").
#
cd packages/
git pull
rm header.pot
cd ..
