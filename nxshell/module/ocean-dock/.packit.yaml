# See the documentation for more information:
# https://packit.dev/docs/configuration/

specfile_path: rpm/lingmo-dock.spec

# add or remove files that should be synced
synced_files:
    - rpm/lingmo-dock.spec
    - .packit.yaml

upstream_package_name: ocean-dock
# downstream (Fedora) RPM package name
downstream_package_name: lingmo-dock

actions:
  fix-spec-file: |
    bash -c "sed -i -r \"0,/Version:/ s/Version:(\s*)\S*/Version:\1${PACKIT_PROJECT_VERSION}/\" rpm/lingmo-dock.spec"
  post-upstream-clone: |
    cp rpm/ocean-dock.spec rpm/lingmo-dock.spec
