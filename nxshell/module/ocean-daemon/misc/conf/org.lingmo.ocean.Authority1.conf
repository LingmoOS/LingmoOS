<?xml version="1.0" encoding="UTF-8"?>
<!-- -*- XML -*- -->
<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN" "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
	<!-- Only lingmo-daemon can own the service -->
	<policy user="lingmo-daemon">
		<allow own="org.lingmo.ocean.Authority1"/>
	</policy>
	<!-- Allow anyone to invoke methods on the interfaces -->
	<policy context="default">
		<allow send_destination="org.lingmo.ocean.Authority1"/>
	</policy>
	<policy user="root">
		<allow send_interface="org.lingmo.ocean.Authority1.Agent"/>
	</policy>
</busconfig>
