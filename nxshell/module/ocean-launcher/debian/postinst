#!/bin/sh
set -e

if [ "$1" = "configure" ];then
    if [ "mips32" = `uname -m` ] || [ "mips64" = `uname -m` ] || [ "mips" = `uname -m` ];then
        cat >/etc/xdg/autostart/ocean-launcher_autostart.desktop <<EOF
[Desktop Entry]
Exec=/usr/bin/ocean-launcher
Name=Lingmo Launcher
OnlyShowIn=Lingmo
StartupNotify=false
Terminal=false
Type=Application
X-GNOME-Autostart-Delay=5
EOF
    fi
fi

#DEBHELPER#

exit 0
