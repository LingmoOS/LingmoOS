#!/bin/bash

qtlupdate=lupdate
qtlrelease=lrelease
qtlconvert=lconvert
tsfile=po/lingmo-boot-maker.ts
$qtlupdate $1 -target-language en_US -ts $tsfile
$qtlconvert -i $tsfile -if ts -o po/lingmo-boot-maker.pot -of po
rm $tsfile

