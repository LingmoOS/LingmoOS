config LINGMO_PACKAGE_LIBMODPLUG
	bool "libmodplug"
	depends on LINGMO_INSTALL_LIBSTDCPP
	help
	  MOD music file decoder

	  http://modplug-xmms.sourceforge.net/

comment "libmodplug needs a toolchain w/ C++"
	depends on !LINGMO_INSTALL_LIBSTDCPP
