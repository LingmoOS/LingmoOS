config LINGMO_PACKAGE_IMX_PARSER
	bool "imx-parser"
	depends on LINGMO_arm || LINGMO_aarch64 # Only relevant for i.MX
	help
	  Binary parser libraries for the NXP i.MX SoCs.

	  This library is provided by NXP as-is and doesn't have
	  an upstream.
