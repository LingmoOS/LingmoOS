comment "rtl8821cu needs a Linux kernel to be built"
	depends on !LINGMO_s390x
	depends on !LINGMO_LINUX_KERNEL

config LINGMO_PACKAGE_RTL8821CU
	bool "rtl8821cu"
	depends on !LINGMO_s390x
	depends on LINGMO_LINUX_KERNEL
	help
	  rtl8821cu wifi driver

	  https://github.com/brektrou/rtl8821CU
