config LINGMO_PACKAGE_GLI
	bool "gli"
	depends on LINGMO_INSTALL_LIBSTDCPP
	help
	  OpenGL Image (GLI)

	  http://gli.g-truc.net

comment "gli needs a toolchain w/ C++"
	depends on !LINGMO_INSTALL_LIBSTDCPP
