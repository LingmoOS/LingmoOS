config LINGMO_PACKAGE_WTFUTIL
	bool "wtfutil"
	depends on LINGMO_USE_MMU  # fork()
	depends on LINGMO_PACKAGE_HOST_GO_TARGET_ARCH_SUPPORTS
	help
	  WTF is the personal information dashboard for your terminal.

	  https://wtfutil.com/
