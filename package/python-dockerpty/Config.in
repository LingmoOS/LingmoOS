config LINGMO_PACKAGE_PYTHON_DOCKERPTY
	bool "python-dockerpty"
	select LINGMO_PACKAGE_PYTHON_SIX # runtime
	help
	  Python library to use the pseudo-tty of a docker container.

	  https://github.com/d11wtq/dockerpty
