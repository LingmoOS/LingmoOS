config LINGMO_PACKAGE_HTPDATE
	bool "htpdate"
	depends on LINGMO_USE_MMU # fork()
	help
	  The HTTP Time Protocol (HTP) is used to synchronize a
	  computer's time with web servers as reference time source.

	  https://github.com/twekkel/htpdate
