config LINGMO_PACKAGE_MBPOLL
	bool "mbpoll"
	select LINGMO_PACKAGE_LIBMODBUS
	help
	  mbpoll is a command line utility to communicate with ModBus
	  slave (RTU or TCP).

	  https://github.com/epsilonrt/mbpoll
