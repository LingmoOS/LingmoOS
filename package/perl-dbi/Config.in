config LINGMO_PACKAGE_PERL_DBI
	bool "perl-dbi"
	depends on !LINGMO_STATIC_LIBS
	help
	  Database independent interface for Perl.

	  http://dbi.perl.org/

comment "perl-dbi needs a toolchain w/ dynamic library"
	depends on LINGMO_STATIC_LIBS
