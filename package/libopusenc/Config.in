config LINGMO_PACKAGE_LIBOPUSENC
	bool "libopusenc"
	select LINGMO_PACKAGE_OPUS
	help
	  The libopusenc library provides a high-level API for
	  encoding opus audio files and live streams.

	  http://opus-codec.org
