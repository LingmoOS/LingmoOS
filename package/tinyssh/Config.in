config LINGMO_PACKAGE_TINYSSH
	bool "tinyssh"
	depends on LINGMO_USE_MMU # fork()
	help
	  TinySSH is a minimalistic SSH server which implements only
	  a subset of SSHv2 features.

	  https://tinyssh.org
