config LINGMO_PACKAGE_LZOP
	bool "lzop"
	select LINGMO_PACKAGE_LZO
	help
	  lzop is a file compressor which is very similar to gzip.
	  lzop uses the LZO data compression library for compression
	  services.

	  http://www.lzop.org/
