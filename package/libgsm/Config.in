config LINGMO_PACKAGE_LIBGSM
	bool "libgsm"
	depends on !LINGMO_STATIC_LIBS
	help
	  Shared libraries for GSM 06.10 lossy speech compression.

	  http://www.quut.com/gsm

comment "libgsm needs a toolchain w/ dynamic library"
	depends on LINGMO_STATIC_LIBS
