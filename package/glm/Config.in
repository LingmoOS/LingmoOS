config LINGMO_PACKAGE_GLM
	bool "glm"
	depends on LINGMO_INSTALL_LIBSTDCPP
	help
	  OpenGL Mathematics (GLM)

	  http://glm.g-truc.net

comment "glm needs a toolchain w/ C++"
	depends on !LINGMO_INSTALL_LIBSTDCPP
