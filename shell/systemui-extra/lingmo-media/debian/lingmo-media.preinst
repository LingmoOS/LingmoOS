#!/bin/bash
  
set -e

path="/usr/lib/`/usr/bin/dpkg-architecture -qDEB_TARGET_MULTIARCH`/lingmo-control-center/libaudio.so"
dpkg-divert --package lingmo-media  --rename --divert "$path"".old" --add $path
