<para>&kappname; programını sisteminizde derlemek ve kurmak için aşağıdakileri &kappname; dağıtımınızın taban dizininde yazın:</para>

<screen><prompt>%</prompt> <userinput><command>./configure</command></userinput>
<prompt>%</prompt> <userinput><command>make</command></userinput>
<prompt>%</prompt> <userinput><command>make</command> install</userinput>
</screen>

<para>&kappname; <command>autoconf</command> and <command>automake</command> kullandığından derlerken sorun çıkmaması gerekir. Sorunlarla karşılaşırsanız lütfen onları &kde; posta listelerinde bildirin.</para>

