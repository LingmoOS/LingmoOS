/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "speechprocessorglue.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.lingmo.aisdk.SpeechProcessor
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:AisdkSpeechProcessor
 * @title: AisdkSpeechProcessor
 * @short_description: Generated C code for the org.lingmo.aisdk.SpeechProcessor D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-lingmo-aisdk-SpeechProcessor.top_of_page">org.lingmo.aisdk.SpeechProcessor</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.lingmo.aisdk.SpeechProcessor ---- */

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_init_IN_ARG_engine_name =
{
  {
    -1,
    (gchar *) "engine_name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_init_IN_ARG_config =
{
  {
    -1,
    (gchar *) "config",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_init_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_init_IN_ARG_engine_name.parent_struct,
  &_aisdk_speech_processor_method_info_init_IN_ARG_config.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_init_OUT_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_init_OUT_ARG_errorCode =
{
  {
    -1,
    (gchar *) "errorCode",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_init_OUT_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_init_OUT_ARG_session_Id.parent_struct,
  &_aisdk_speech_processor_method_info_init_OUT_ARG_errorCode.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_init =
{
  {
    -1,
    (gchar *) "init",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_init_IN_ARG_pointers,
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_init_OUT_ARG_pointers,
    NULL
  },
  "handle-init",
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_start_recognizing_audio_IN_ARG_audioProperties =
{
  {
    -1,
    (gchar *) "audioProperties",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_start_recognizing_audio_IN_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_start_recognizing_audio_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_start_recognizing_audio_IN_ARG_audioProperties.parent_struct,
  &_aisdk_speech_processor_method_info_start_recognizing_audio_IN_ARG_session_Id.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_start_recognizing_audio_OUT_ARG_errorCode =
{
  {
    -1,
    (gchar *) "errorCode",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_start_recognizing_audio_OUT_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_start_recognizing_audio_OUT_ARG_errorCode.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_start_recognizing_audio =
{
  {
    -1,
    (gchar *) "startRecognizingAudio",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_start_recognizing_audio_IN_ARG_pointers,
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_start_recognizing_audio_OUT_ARG_pointers,
    NULL
  },
  "handle-start-recognizing-audio",
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_audio_data_IN_ARG_audioData =
{
  {
    -1,
    (gchar *) "audioData",
    (gchar *) "ay",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_audio_data_IN_ARG_audioDataLength =
{
  {
    -1,
    (gchar *) "audioDataLength",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_audio_data_IN_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_input_audio_data_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_input_audio_data_IN_ARG_audioData.parent_struct,
  &_aisdk_speech_processor_method_info_input_audio_data_IN_ARG_audioDataLength.parent_struct,
  &_aisdk_speech_processor_method_info_input_audio_data_IN_ARG_session_Id.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_textData =
{
  {
    -1,
    (gchar *) "textData",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_textDataLength =
{
  {
    -1,
    (gchar *) "textDataLength",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_speakerId =
{
  {
    -1,
    (gchar *) "speakerId",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_errorCode =
{
  {
    -1,
    (gchar *) "errorCode",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_textData.parent_struct,
  &_aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_textDataLength.parent_struct,
  &_aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_speakerId.parent_struct,
  &_aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_errorCode.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_input_audio_data =
{
  {
    -1,
    (gchar *) "inputAudioData",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_input_audio_data_IN_ARG_pointers,
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_input_audio_data_OUT_ARG_pointers,
    NULL
  },
  "handle-input-audio-data",
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_stop_recognizing_audio_IN_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_stop_recognizing_audio_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_stop_recognizing_audio_IN_ARG_session_Id.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_stop_recognizing_audio =
{
  {
    -1,
    (gchar *) "stopRecognizingAudio",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_stop_recognizing_audio_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-stop-recognizing-audio",
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_audioProperties =
{
  {
    -1,
    (gchar *) "audioProperties",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_audioData =
{
  {
    -1,
    (gchar *) "audioData",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_audioDataLength =
{
  {
    -1,
    (gchar *) "audioDataLength",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_audioProperties.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_audioData.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_audioDataLength.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_session_Id.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_textData =
{
  {
    -1,
    (gchar *) "textData",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_textDataLength =
{
  {
    -1,
    (gchar *) "textDataLength",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_speakerId =
{
  {
    -1,
    (gchar *) "speakerId",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_errorCode =
{
  {
    -1,
    (gchar *) "errorCode",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_textData.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_textDataLength.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_speakerId.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_errorCode.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_recognizing_complete_audio =
{
  {
    -1,
    (gchar *) "recognizingCompleteAudio",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_recognizing_complete_audio_IN_ARG_pointers,
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_recognizing_complete_audio_OUT_ARG_pointers,
    NULL
  },
  "handle-recognizing-complete-audio",
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_start_synthesizing_audio_IN_ARG_voiceProperties =
{
  {
    -1,
    (gchar *) "voiceProperties",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_start_synthesizing_audio_IN_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_start_synthesizing_audio_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_start_synthesizing_audio_IN_ARG_voiceProperties.parent_struct,
  &_aisdk_speech_processor_method_info_start_synthesizing_audio_IN_ARG_session_Id.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_start_synthesizing_audio_OUT_ARG_errorCode =
{
  {
    -1,
    (gchar *) "errorCode",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_start_synthesizing_audio_OUT_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_start_synthesizing_audio_OUT_ARG_errorCode.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_start_synthesizing_audio =
{
  {
    -1,
    (gchar *) "startSynthesizingAudio",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_start_synthesizing_audio_IN_ARG_pointers,
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_start_synthesizing_audio_OUT_ARG_pointers,
    NULL
  },
  "handle-start-synthesizing-audio",
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_continuous_text_IN_ARG_text =
{
  {
    -1,
    (gchar *) "text",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_continuous_text_IN_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_input_continuous_text_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_input_continuous_text_IN_ARG_text.parent_struct,
  &_aisdk_speech_processor_method_info_input_continuous_text_IN_ARG_session_Id.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioFormat =
{
  {
    -1,
    (gchar *) "audioFormat",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioRate =
{
  {
    -1,
    (gchar *) "audioRate",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioChannel =
{
  {
    -1,
    (gchar *) "audioChannel",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioData =
{
  {
    -1,
    (gchar *) "audioData",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioDataLength =
{
  {
    -1,
    (gchar *) "audioDataLength",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_errorCode =
{
  {
    -1,
    (gchar *) "errorCode",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioFormat.parent_struct,
  &_aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioRate.parent_struct,
  &_aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioChannel.parent_struct,
  &_aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioData.parent_struct,
  &_aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_audioDataLength.parent_struct,
  &_aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_errorCode.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_input_continuous_text =
{
  {
    -1,
    (gchar *) "inputContinuousText",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_input_continuous_text_IN_ARG_pointers,
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_input_continuous_text_OUT_ARG_pointers,
    NULL
  },
  "handle-input-continuous-text",
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_stop_synthesizing_audio_IN_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_stop_synthesizing_audio_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_stop_synthesizing_audio_IN_ARG_session_Id.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_stop_synthesizing_audio =
{
  {
    -1,
    (gchar *) "stopSynthesizingAudio",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_stop_synthesizing_audio_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-stop-synthesizing-audio",
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_IN_ARG_voiceProperties =
{
  {
    -1,
    (gchar *) "voiceProperties",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_IN_ARG_text =
{
  {
    -1,
    (gchar *) "text",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_IN_ARG_session_Id =
{
  {
    -1,
    (gchar *) "session_Id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_recognizing_complete_text_IN_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_recognizing_complete_text_IN_ARG_voiceProperties.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_text_IN_ARG_text.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_text_IN_ARG_session_Id.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioFormat =
{
  {
    -1,
    (gchar *) "audioFormat",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioRate =
{
  {
    -1,
    (gchar *) "audioRate",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioChannel =
{
  {
    -1,
    (gchar *) "audioChannel",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioData =
{
  {
    -1,
    (gchar *) "audioData",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioDataLength =
{
  {
    -1,
    (gchar *) "audioDataLength",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_errorMsg =
{
  {
    -1,
    (gchar *) "errorMsg",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_pointers[] =
{
  &_aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioFormat.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioRate.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioChannel.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioData.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_audioDataLength.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_errorMsg.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _aisdk_speech_processor_method_info_recognizing_complete_text =
{
  {
    -1,
    (gchar *) "recognizingCompleteText",
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_recognizing_complete_text_IN_ARG_pointers,
    (GDBusArgInfo **) &_aisdk_speech_processor_method_info_recognizing_complete_text_OUT_ARG_pointers,
    NULL
  },
  "handle-recognizing-complete-text",
  FALSE
};

static const GDBusMethodInfo * const _aisdk_speech_processor_method_info_pointers[] =
{
  &_aisdk_speech_processor_method_info_init.parent_struct,
  &_aisdk_speech_processor_method_info_start_recognizing_audio.parent_struct,
  &_aisdk_speech_processor_method_info_input_audio_data.parent_struct,
  &_aisdk_speech_processor_method_info_stop_recognizing_audio.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_audio.parent_struct,
  &_aisdk_speech_processor_method_info_start_synthesizing_audio.parent_struct,
  &_aisdk_speech_processor_method_info_input_continuous_text.parent_struct,
  &_aisdk_speech_processor_method_info_stop_synthesizing_audio.parent_struct,
  &_aisdk_speech_processor_method_info_recognizing_complete_text.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _aisdk_speech_processor_interface_info =
{
  {
    -1,
    (gchar *) "org.lingmo.aisdk.SpeechProcessor",
    (GDBusMethodInfo **) &_aisdk_speech_processor_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "aisdk-speech-processor",
};


/**
 * aisdk_speech_processor_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-lingmo-aisdk-SpeechProcessor.top_of_page">org.lingmo.aisdk.SpeechProcessor</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
aisdk_speech_processor_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_aisdk_speech_processor_interface_info.parent_struct;
}

/**
 * aisdk_speech_processor_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #AisdkSpeechProcessor interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
aisdk_speech_processor_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * AisdkSpeechProcessor:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-lingmo-aisdk-SpeechProcessor.top_of_page">org.lingmo.aisdk.SpeechProcessor</link>.
 */

/**
 * AisdkSpeechProcessorIface:
 * @parent_iface: The parent interface.
 * @handle_init: Handler for the #AisdkSpeechProcessor::handle-init signal.
 * @handle_input_audio_data: Handler for the #AisdkSpeechProcessor::handle-input-audio-data signal.
 * @handle_input_continuous_text: Handler for the #AisdkSpeechProcessor::handle-input-continuous-text signal.
 * @handle_recognizing_complete_audio: Handler for the #AisdkSpeechProcessor::handle-recognizing-complete-audio signal.
 * @handle_recognizing_complete_text: Handler for the #AisdkSpeechProcessor::handle-recognizing-complete-text signal.
 * @handle_start_recognizing_audio: Handler for the #AisdkSpeechProcessor::handle-start-recognizing-audio signal.
 * @handle_start_synthesizing_audio: Handler for the #AisdkSpeechProcessor::handle-start-synthesizing-audio signal.
 * @handle_stop_recognizing_audio: Handler for the #AisdkSpeechProcessor::handle-stop-recognizing-audio signal.
 * @handle_stop_synthesizing_audio: Handler for the #AisdkSpeechProcessor::handle-stop-synthesizing-audio signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-lingmo-aisdk-SpeechProcessor.top_of_page">org.lingmo.aisdk.SpeechProcessor</link>.
 */

typedef AisdkSpeechProcessorIface AisdkSpeechProcessorInterface;
G_DEFINE_INTERFACE (AisdkSpeechProcessor, aisdk_speech_processor, G_TYPE_OBJECT)

static void
aisdk_speech_processor_default_init (AisdkSpeechProcessorIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * AisdkSpeechProcessor::handle-init:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_engine_name: Argument passed by remote caller.
   * @arg_config: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.init">init()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_init() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-init",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_init),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * AisdkSpeechProcessor::handle-start-recognizing-audio:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_audioProperties: Argument passed by remote caller.
   * @arg_session_Id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.startRecognizingAudio">startRecognizingAudio()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_start_recognizing_audio() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-recognizing-audio",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_start_recognizing_audio),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT);

  /**
   * AisdkSpeechProcessor::handle-input-audio-data:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_audioData: Argument passed by remote caller.
   * @arg_audioDataLength: Argument passed by remote caller.
   * @arg_session_Id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.inputAudioData">inputAudioData()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_input_audio_data() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-input-audio-data",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_input_audio_data),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT, G_TYPE_INT);

  /**
   * AisdkSpeechProcessor::handle-stop-recognizing-audio:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_session_Id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.stopRecognizingAudio">stopRecognizingAudio()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_stop_recognizing_audio() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-recognizing-audio",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_stop_recognizing_audio),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT);

  /**
   * AisdkSpeechProcessor::handle-recognizing-complete-audio:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_audioProperties: Argument passed by remote caller.
   * @arg_audioData: Argument passed by remote caller.
   * @arg_audioDataLength: Argument passed by remote caller.
   * @arg_session_Id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.recognizingCompleteAudio">recognizingCompleteAudio()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_recognizing_complete_audio() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-recognizing-complete-audio",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_recognizing_complete_audio),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    5,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_INT, G_TYPE_INT);

  /**
   * AisdkSpeechProcessor::handle-start-synthesizing-audio:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_voiceProperties: Argument passed by remote caller.
   * @arg_session_Id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.startSynthesizingAudio">startSynthesizingAudio()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_start_synthesizing_audio() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-synthesizing-audio",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_start_synthesizing_audio),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT);

  /**
   * AisdkSpeechProcessor::handle-input-continuous-text:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_text: Argument passed by remote caller.
   * @arg_session_Id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.inputContinuousText">inputContinuousText()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_input_continuous_text() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-input-continuous-text",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_input_continuous_text),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT);

  /**
   * AisdkSpeechProcessor::handle-stop-synthesizing-audio:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_session_Id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.stopSynthesizingAudio">stopSynthesizingAudio()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_stop_synthesizing_audio() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-synthesizing-audio",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_stop_synthesizing_audio),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT);

  /**
   * AisdkSpeechProcessor::handle-recognizing-complete-text:
   * @object: A #AisdkSpeechProcessor.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_voiceProperties: Argument passed by remote caller.
   * @arg_text: Argument passed by remote caller.
   * @arg_session_Id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.recognizingCompleteText">recognizingCompleteText()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call aisdk_speech_processor_complete_recognizing_complete_text() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-recognizing-complete-text",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (AisdkSpeechProcessorIface, handle_recognizing_complete_text),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_INT);

}

/**
 * aisdk_speech_processor_call_init:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_engine_name: Argument to pass with the method invocation.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.init">init()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_init_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_init_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_init (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_engine_name,
    const gchar *arg_config,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "init",
    g_variant_new ("(ss)",
                   arg_engine_name,
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_init_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @out_session_Id: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_init().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_init().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_init_finish (
    AisdkSpeechProcessor *proxy,
    gint *out_session_Id,
    gint *out_errorCode,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(ii)",
                 out_session_Id,
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_init_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_engine_name: Argument to pass with the method invocation.
 * @arg_config: Argument to pass with the method invocation.
 * @out_session_Id: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.init">init()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_init() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_init_sync (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_engine_name,
    const gchar *arg_config,
    gint *out_session_Id,
    gint *out_errorCode,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "init",
    g_variant_new ("(ss)",
                   arg_engine_name,
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(ii)",
                 out_session_Id,
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_start_recognizing_audio:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_audioProperties: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.startRecognizingAudio">startRecognizingAudio()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_start_recognizing_audio_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_start_recognizing_audio_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_start_recognizing_audio (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_audioProperties,
    gint arg_session_Id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "startRecognizingAudio",
    g_variant_new ("(si)",
                   arg_audioProperties,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_start_recognizing_audio_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_start_recognizing_audio().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_start_recognizing_audio().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_start_recognizing_audio_finish (
    AisdkSpeechProcessor *proxy,
    gint *out_errorCode,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_start_recognizing_audio_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_audioProperties: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.startRecognizingAudio">startRecognizingAudio()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_start_recognizing_audio() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_start_recognizing_audio_sync (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_audioProperties,
    gint arg_session_Id,
    gint *out_errorCode,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "startRecognizingAudio",
    g_variant_new ("(si)",
                   arg_audioProperties,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_input_audio_data:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_audioData: Argument to pass with the method invocation.
 * @arg_audioDataLength: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.inputAudioData">inputAudioData()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_input_audio_data_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_input_audio_data_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_input_audio_data (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_audioData,
    gint arg_audioDataLength,
    gint arg_session_Id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "inputAudioData",
    g_variant_new ("(^ayii)",
                   arg_audioData,
                   arg_audioDataLength,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_input_audio_data_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @out_textData: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_textDataLength: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_speakerId: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_input_audio_data().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_input_audio_data().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_input_audio_data_finish (
    AisdkSpeechProcessor *proxy,
    gchar **out_textData,
    gint *out_textDataLength,
    gint *out_speakerId,
    gint *out_errorCode,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(siii)",
                 out_textData,
                 out_textDataLength,
                 out_speakerId,
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_input_audio_data_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_audioData: Argument to pass with the method invocation.
 * @arg_audioDataLength: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @out_textData: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_textDataLength: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_speakerId: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.inputAudioData">inputAudioData()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_input_audio_data() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_input_audio_data_sync (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_audioData,
    gint arg_audioDataLength,
    gint arg_session_Id,
    gchar **out_textData,
    gint *out_textDataLength,
    gint *out_speakerId,
    gint *out_errorCode,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "inputAudioData",
    g_variant_new ("(^ayii)",
                   arg_audioData,
                   arg_audioDataLength,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(siii)",
                 out_textData,
                 out_textDataLength,
                 out_speakerId,
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_stop_recognizing_audio:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.stopRecognizingAudio">stopRecognizingAudio()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_stop_recognizing_audio_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_stop_recognizing_audio_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_stop_recognizing_audio (
    AisdkSpeechProcessor *proxy,
    gint arg_session_Id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "stopRecognizingAudio",
    g_variant_new ("(i)",
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_stop_recognizing_audio_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_stop_recognizing_audio().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_stop_recognizing_audio().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_stop_recognizing_audio_finish (
    AisdkSpeechProcessor *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_stop_recognizing_audio_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.stopRecognizingAudio">stopRecognizingAudio()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_stop_recognizing_audio() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_stop_recognizing_audio_sync (
    AisdkSpeechProcessor *proxy,
    gint arg_session_Id,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "stopRecognizingAudio",
    g_variant_new ("(i)",
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_recognizing_complete_audio:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_audioProperties: Argument to pass with the method invocation.
 * @arg_audioData: Argument to pass with the method invocation.
 * @arg_audioDataLength: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.recognizingCompleteAudio">recognizingCompleteAudio()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_recognizing_complete_audio_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_recognizing_complete_audio_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_recognizing_complete_audio (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_audioProperties,
    const gchar *arg_audioData,
    gint arg_audioDataLength,
    gint arg_session_Id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "recognizingCompleteAudio",
    g_variant_new ("(ssii)",
                   arg_audioProperties,
                   arg_audioData,
                   arg_audioDataLength,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_recognizing_complete_audio_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @out_textData: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_textDataLength: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_speakerId: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_recognizing_complete_audio().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_recognizing_complete_audio().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_recognizing_complete_audio_finish (
    AisdkSpeechProcessor *proxy,
    gchar **out_textData,
    gint *out_textDataLength,
    gint *out_speakerId,
    gint *out_errorCode,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(siii)",
                 out_textData,
                 out_textDataLength,
                 out_speakerId,
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_recognizing_complete_audio_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_audioProperties: Argument to pass with the method invocation.
 * @arg_audioData: Argument to pass with the method invocation.
 * @arg_audioDataLength: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @out_textData: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_textDataLength: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_speakerId: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.recognizingCompleteAudio">recognizingCompleteAudio()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_recognizing_complete_audio() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_recognizing_complete_audio_sync (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_audioProperties,
    const gchar *arg_audioData,
    gint arg_audioDataLength,
    gint arg_session_Id,
    gchar **out_textData,
    gint *out_textDataLength,
    gint *out_speakerId,
    gint *out_errorCode,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "recognizingCompleteAudio",
    g_variant_new ("(ssii)",
                   arg_audioProperties,
                   arg_audioData,
                   arg_audioDataLength,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(siii)",
                 out_textData,
                 out_textDataLength,
                 out_speakerId,
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_start_synthesizing_audio:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_voiceProperties: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.startSynthesizingAudio">startSynthesizingAudio()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_start_synthesizing_audio_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_start_synthesizing_audio_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_start_synthesizing_audio (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_voiceProperties,
    gint arg_session_Id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "startSynthesizingAudio",
    g_variant_new ("(si)",
                   arg_voiceProperties,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_start_synthesizing_audio_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_start_synthesizing_audio().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_start_synthesizing_audio().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_start_synthesizing_audio_finish (
    AisdkSpeechProcessor *proxy,
    gint *out_errorCode,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_start_synthesizing_audio_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_voiceProperties: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.startSynthesizingAudio">startSynthesizingAudio()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_start_synthesizing_audio() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_start_synthesizing_audio_sync (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_voiceProperties,
    gint arg_session_Id,
    gint *out_errorCode,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "startSynthesizingAudio",
    g_variant_new ("(si)",
                   arg_voiceProperties,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_input_continuous_text:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_text: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.inputContinuousText">inputContinuousText()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_input_continuous_text_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_input_continuous_text_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_input_continuous_text (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_text,
    gint arg_session_Id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "inputContinuousText",
    g_variant_new ("(si)",
                   arg_text,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_input_continuous_text_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @out_audioFormat: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioRate: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioChannel: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioData: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioDataLength: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_input_continuous_text().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_input_continuous_text().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_input_continuous_text_finish (
    AisdkSpeechProcessor *proxy,
    gchar **out_audioFormat,
    gint *out_audioRate,
    gint *out_audioChannel,
    gchar **out_audioData,
    gint *out_audioDataLength,
    gint *out_errorCode,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(siisii)",
                 out_audioFormat,
                 out_audioRate,
                 out_audioChannel,
                 out_audioData,
                 out_audioDataLength,
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_input_continuous_text_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_text: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @out_audioFormat: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioRate: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioChannel: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioData: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioDataLength: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorCode: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.inputContinuousText">inputContinuousText()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_input_continuous_text() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_input_continuous_text_sync (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_text,
    gint arg_session_Id,
    gchar **out_audioFormat,
    gint *out_audioRate,
    gint *out_audioChannel,
    gchar **out_audioData,
    gint *out_audioDataLength,
    gint *out_errorCode,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "inputContinuousText",
    g_variant_new ("(si)",
                   arg_text,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(siisii)",
                 out_audioFormat,
                 out_audioRate,
                 out_audioChannel,
                 out_audioData,
                 out_audioDataLength,
                 out_errorCode);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_stop_synthesizing_audio:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.stopSynthesizingAudio">stopSynthesizingAudio()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_stop_synthesizing_audio_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_stop_synthesizing_audio_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_stop_synthesizing_audio (
    AisdkSpeechProcessor *proxy,
    gint arg_session_Id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "stopSynthesizingAudio",
    g_variant_new ("(i)",
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_stop_synthesizing_audio_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_stop_synthesizing_audio().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_stop_synthesizing_audio().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_stop_synthesizing_audio_finish (
    AisdkSpeechProcessor *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_stop_synthesizing_audio_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.stopSynthesizingAudio">stopSynthesizingAudio()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_stop_synthesizing_audio() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_stop_synthesizing_audio_sync (
    AisdkSpeechProcessor *proxy,
    gint arg_session_Id,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "stopSynthesizingAudio",
    g_variant_new ("(i)",
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_recognizing_complete_text:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_voiceProperties: Argument to pass with the method invocation.
 * @arg_text: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.recognizingCompleteText">recognizingCompleteText()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_call_recognizing_complete_text_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_call_recognizing_complete_text_sync() for the synchronous, blocking version of this method.
 */
void
aisdk_speech_processor_call_recognizing_complete_text (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_voiceProperties,
    const gchar *arg_text,
    gint arg_session_Id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "recognizingCompleteText",
    g_variant_new ("(ssi)",
                   arg_voiceProperties,
                   arg_text,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * aisdk_speech_processor_call_recognizing_complete_text_finish:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @out_audioFormat: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioRate: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioChannel: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioData: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioDataLength: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorMsg: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_call_recognizing_complete_text().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with aisdk_speech_processor_call_recognizing_complete_text().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_recognizing_complete_text_finish (
    AisdkSpeechProcessor *proxy,
    gchar **out_audioFormat,
    gint *out_audioRate,
    gint *out_audioChannel,
    gchar **out_audioData,
    gint *out_audioDataLength,
    gint *out_errorMsg,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(siisii)",
                 out_audioFormat,
                 out_audioRate,
                 out_audioChannel,
                 out_audioData,
                 out_audioDataLength,
                 out_errorMsg);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_call_recognizing_complete_text_sync:
 * @proxy: A #AisdkSpeechProcessorProxy.
 * @arg_voiceProperties: Argument to pass with the method invocation.
 * @arg_text: Argument to pass with the method invocation.
 * @arg_session_Id: Argument to pass with the method invocation.
 * @out_audioFormat: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioRate: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioChannel: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioData: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_audioDataLength: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_errorMsg: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.recognizingCompleteText">recognizingCompleteText()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_call_recognizing_complete_text() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
aisdk_speech_processor_call_recognizing_complete_text_sync (
    AisdkSpeechProcessor *proxy,
    const gchar *arg_voiceProperties,
    const gchar *arg_text,
    gint arg_session_Id,
    gchar **out_audioFormat,
    gint *out_audioRate,
    gint *out_audioChannel,
    gchar **out_audioData,
    gint *out_audioDataLength,
    gint *out_errorMsg,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "recognizingCompleteText",
    g_variant_new ("(ssi)",
                   arg_voiceProperties,
                   arg_text,
                   arg_session_Id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(siisii)",
                 out_audioFormat,
                 out_audioRate,
                 out_audioChannel,
                 out_audioData,
                 out_audioDataLength,
                 out_errorMsg);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * aisdk_speech_processor_complete_init:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @session_Id: Parameter to return.
 * @errorCode: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.init">init()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_init (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation,
    gint session_Id,
    gint errorCode)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(ii)",
                   session_Id,
                   errorCode));
}

/**
 * aisdk_speech_processor_complete_start_recognizing_audio:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @errorCode: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.startRecognizingAudio">startRecognizingAudio()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_start_recognizing_audio (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation,
    gint errorCode)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(i)",
                   errorCode));
}

/**
 * aisdk_speech_processor_complete_input_audio_data:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @textData: Parameter to return.
 * @textDataLength: Parameter to return.
 * @speakerId: Parameter to return.
 * @errorCode: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.inputAudioData">inputAudioData()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_input_audio_data (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation,
    const gchar *textData,
    gint textDataLength,
    gint speakerId,
    gint errorCode)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(siii)",
                   textData,
                   textDataLength,
                   speakerId,
                   errorCode));
}

/**
 * aisdk_speech_processor_complete_stop_recognizing_audio:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.stopRecognizingAudio">stopRecognizingAudio()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_stop_recognizing_audio (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * aisdk_speech_processor_complete_recognizing_complete_audio:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @textData: Parameter to return.
 * @textDataLength: Parameter to return.
 * @speakerId: Parameter to return.
 * @errorCode: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.recognizingCompleteAudio">recognizingCompleteAudio()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_recognizing_complete_audio (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation,
    const gchar *textData,
    gint textDataLength,
    gint speakerId,
    gint errorCode)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(siii)",
                   textData,
                   textDataLength,
                   speakerId,
                   errorCode));
}

/**
 * aisdk_speech_processor_complete_start_synthesizing_audio:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @errorCode: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.startSynthesizingAudio">startSynthesizingAudio()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_start_synthesizing_audio (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation,
    gint errorCode)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(i)",
                   errorCode));
}

/**
 * aisdk_speech_processor_complete_input_continuous_text:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @audioFormat: Parameter to return.
 * @audioRate: Parameter to return.
 * @audioChannel: Parameter to return.
 * @audioData: Parameter to return.
 * @audioDataLength: Parameter to return.
 * @errorCode: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.inputContinuousText">inputContinuousText()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_input_continuous_text (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation,
    const gchar *audioFormat,
    gint audioRate,
    gint audioChannel,
    const gchar *audioData,
    gint audioDataLength,
    gint errorCode)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(siisii)",
                   audioFormat,
                   audioRate,
                   audioChannel,
                   audioData,
                   audioDataLength,
                   errorCode));
}

/**
 * aisdk_speech_processor_complete_stop_synthesizing_audio:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.stopSynthesizingAudio">stopSynthesizingAudio()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_stop_synthesizing_audio (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * aisdk_speech_processor_complete_recognizing_complete_text:
 * @object: A #AisdkSpeechProcessor.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @audioFormat: Parameter to return.
 * @audioRate: Parameter to return.
 * @audioChannel: Parameter to return.
 * @audioData: Parameter to return.
 * @audioDataLength: Parameter to return.
 * @errorMsg: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-lingmo-aisdk-SpeechProcessor.recognizingCompleteText">recognizingCompleteText()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
aisdk_speech_processor_complete_recognizing_complete_text (
    AisdkSpeechProcessor *object,
    GDBusMethodInvocation *invocation,
    const gchar *audioFormat,
    gint audioRate,
    gint audioChannel,
    const gchar *audioData,
    gint audioDataLength,
    gint errorMsg)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(siisii)",
                   audioFormat,
                   audioRate,
                   audioChannel,
                   audioData,
                   audioDataLength,
                   errorMsg));
}

/* ------------------------------------------------------------------------ */

/**
 * AisdkSpeechProcessorProxy:
 *
 * The #AisdkSpeechProcessorProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * AisdkSpeechProcessorProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #AisdkSpeechProcessorProxy.
 */

struct _AisdkSpeechProcessorProxyPrivate
{
  GData *qdata;
};

static void aisdk_speech_processor_proxy_iface_init (AisdkSpeechProcessorIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (AisdkSpeechProcessorProxy, aisdk_speech_processor_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (AisdkSpeechProcessorProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_AISDK_SPEECH_PROCESSOR, aisdk_speech_processor_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (AisdkSpeechProcessorProxy, aisdk_speech_processor_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_AISDK_SPEECH_PROCESSOR, aisdk_speech_processor_proxy_iface_init))

#endif
static void
aisdk_speech_processor_proxy_finalize (GObject *object)
{
  AisdkSpeechProcessorProxy *proxy = AISDK_SPEECH_PROCESSOR_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (aisdk_speech_processor_proxy_parent_class)->finalize (object);
}

static void
aisdk_speech_processor_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
aisdk_speech_processor_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
aisdk_speech_processor_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_aisdk_speech_processor_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_AISDK_SPEECH_PROCESSOR);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_AISDK_SPEECH_PROCESSOR);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
aisdk_speech_processor_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  AisdkSpeechProcessorProxy *proxy = AISDK_SPEECH_PROCESSOR_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_aisdk_speech_processor_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_aisdk_speech_processor_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
aisdk_speech_processor_proxy_init (AisdkSpeechProcessorProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = aisdk_speech_processor_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_AISDK_SPEECH_PROCESSOR_PROXY, AisdkSpeechProcessorProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), aisdk_speech_processor_interface_info ());
}

static void
aisdk_speech_processor_proxy_class_init (AisdkSpeechProcessorProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = aisdk_speech_processor_proxy_finalize;
  gobject_class->get_property = aisdk_speech_processor_proxy_get_property;
  gobject_class->set_property = aisdk_speech_processor_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = aisdk_speech_processor_proxy_g_signal;
  proxy_class->g_properties_changed = aisdk_speech_processor_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (AisdkSpeechProcessorProxyPrivate));
#endif
}

static void
aisdk_speech_processor_proxy_iface_init (AisdkSpeechProcessorIface *iface)
{
}

/**
 * aisdk_speech_processor_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-lingmo-aisdk-SpeechProcessor.top_of_page">org.lingmo.aisdk.SpeechProcessor</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_proxy_new_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
aisdk_speech_processor_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_AISDK_SPEECH_PROCESSOR_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.lingmo.aisdk.SpeechProcessor", NULL);
}

/**
 * aisdk_speech_processor_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with aisdk_speech_processor_proxy_new().
 *
 * Returns: (transfer full) (type AisdkSpeechProcessorProxy): The constructed proxy object or %NULL if @error is set.
 */
AisdkSpeechProcessor *
aisdk_speech_processor_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return AISDK_SPEECH_PROCESSOR (ret);
  else
    return NULL;
}

/**
 * aisdk_speech_processor_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-lingmo-aisdk-SpeechProcessor.top_of_page">org.lingmo.aisdk.SpeechProcessor</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type AisdkSpeechProcessorProxy): The constructed proxy object or %NULL if @error is set.
 */
AisdkSpeechProcessor *
aisdk_speech_processor_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_AISDK_SPEECH_PROCESSOR_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.lingmo.aisdk.SpeechProcessor", NULL);
  if (ret != NULL)
    return AISDK_SPEECH_PROCESSOR (ret);
  else
    return NULL;
}


/**
 * aisdk_speech_processor_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like aisdk_speech_processor_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call aisdk_speech_processor_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See aisdk_speech_processor_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
aisdk_speech_processor_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_AISDK_SPEECH_PROCESSOR_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.lingmo.aisdk.SpeechProcessor", NULL);
}

/**
 * aisdk_speech_processor_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to aisdk_speech_processor_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with aisdk_speech_processor_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type AisdkSpeechProcessorProxy): The constructed proxy object or %NULL if @error is set.
 */
AisdkSpeechProcessor *
aisdk_speech_processor_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return AISDK_SPEECH_PROCESSOR (ret);
  else
    return NULL;
}

/**
 * aisdk_speech_processor_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like aisdk_speech_processor_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See aisdk_speech_processor_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type AisdkSpeechProcessorProxy): The constructed proxy object or %NULL if @error is set.
 */
AisdkSpeechProcessor *
aisdk_speech_processor_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_AISDK_SPEECH_PROCESSOR_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.lingmo.aisdk.SpeechProcessor", NULL);
  if (ret != NULL)
    return AISDK_SPEECH_PROCESSOR (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * AisdkSpeechProcessorSkeleton:
 *
 * The #AisdkSpeechProcessorSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * AisdkSpeechProcessorSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #AisdkSpeechProcessorSkeleton.
 */

struct _AisdkSpeechProcessorSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_aisdk_speech_processor_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  AisdkSpeechProcessorSkeleton *skeleton = AISDK_SPEECH_PROCESSOR_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_AISDK_SPEECH_PROCESSOR);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_AISDK_SPEECH_PROCESSOR);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_aisdk_speech_processor_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  AisdkSpeechProcessorSkeleton *skeleton = AISDK_SPEECH_PROCESSOR_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_aisdk_speech_processor_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_aisdk_speech_processor_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  AisdkSpeechProcessorSkeleton *skeleton = AISDK_SPEECH_PROCESSOR_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_aisdk_speech_processor_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _aisdk_speech_processor_skeleton_vtable =
{
  _aisdk_speech_processor_skeleton_handle_method_call,
  _aisdk_speech_processor_skeleton_handle_get_property,
  _aisdk_speech_processor_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
aisdk_speech_processor_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return aisdk_speech_processor_interface_info ();
}

static GDBusInterfaceVTable *
aisdk_speech_processor_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_aisdk_speech_processor_skeleton_vtable;
}

static GVariant *
aisdk_speech_processor_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  AisdkSpeechProcessorSkeleton *skeleton = AISDK_SPEECH_PROCESSOR_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_aisdk_speech_processor_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _aisdk_speech_processor_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _aisdk_speech_processor_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _aisdk_speech_processor_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.lingmo.aisdk.SpeechProcessor", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
aisdk_speech_processor_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void aisdk_speech_processor_skeleton_iface_init (AisdkSpeechProcessorIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (AisdkSpeechProcessorSkeleton, aisdk_speech_processor_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (AisdkSpeechProcessorSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_AISDK_SPEECH_PROCESSOR, aisdk_speech_processor_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (AisdkSpeechProcessorSkeleton, aisdk_speech_processor_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_AISDK_SPEECH_PROCESSOR, aisdk_speech_processor_skeleton_iface_init))

#endif
static void
aisdk_speech_processor_skeleton_finalize (GObject *object)
{
  AisdkSpeechProcessorSkeleton *skeleton = AISDK_SPEECH_PROCESSOR_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (aisdk_speech_processor_skeleton_parent_class)->finalize (object);
}

static void
aisdk_speech_processor_skeleton_init (AisdkSpeechProcessorSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = aisdk_speech_processor_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_AISDK_SPEECH_PROCESSOR_SKELETON, AisdkSpeechProcessorSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
aisdk_speech_processor_skeleton_class_init (AisdkSpeechProcessorSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = aisdk_speech_processor_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = aisdk_speech_processor_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = aisdk_speech_processor_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = aisdk_speech_processor_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = aisdk_speech_processor_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (AisdkSpeechProcessorSkeletonPrivate));
#endif
}

static void
aisdk_speech_processor_skeleton_iface_init (AisdkSpeechProcessorIface *iface)
{
}

/**
 * aisdk_speech_processor_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-lingmo-aisdk-SpeechProcessor.top_of_page">org.lingmo.aisdk.SpeechProcessor</link>.
 *
 * Returns: (transfer full) (type AisdkSpeechProcessorSkeleton): The skeleton object.
 */
AisdkSpeechProcessor *
aisdk_speech_processor_skeleton_new (void)
{
  return AISDK_SPEECH_PROCESSOR (g_object_new (TYPE_AISDK_SPEECH_PROCESSOR_SKELETON, NULL));
}

